<common:BasePage
    x:Class="PlantSitter_Resp.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlantSitter_Resp"
    xmlns:uc="using:PlantSitter_Resp.UC"
    xmlns:common="using:PlantSitter_Resp.Common"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:triggers="using:JP.CustomTriggers"
    xmlns:attach="using:JP.Utils.Framework"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Interactivity:Interaction.Behaviors>
        <Core:EventTriggerBehavior EventName="GlobalPageKeyDown">
            <attach:InvokeCommandByKeyDown Command="{x:Bind MainVM.ConfirmActionCommand}" PressedKey="Enter" />
        </Core:EventTriggerBehavior>
    </Interactivity:Interaction.Behaviors>
    <Grid Background="{StaticResource PlantSitterDashboardColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid x:Name="TitleGrid" Background="{StaticResource PlantSitterDashboardDarkColor}">
            <TextBlock FontSize="20" Margin="10" Text="PlantSitter | 你的植物保姆" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        </Grid>
        <uc:LoginControl x:Name="LoginControl" DataContext="{x:Bind MainVM,Mode=OneWay}" Grid.Row="1"/>
        <uc:DashboardControl x:Name="DashboardControl"/>
    </Grid>
</common:BasePage>